<div class="header">
  <div class="container">
    <%= link_to t('admini.title'), '#', class: 'header-title' %>
    <%= link_to t('admini.menu'), '#' %>
    <div class="right">
      <%= link_to t('admini.logout'), '#' %>
    </div>
  </div>
</div>
